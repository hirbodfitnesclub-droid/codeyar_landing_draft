# tasks.md

## Task 1: بهینه‌سازی Navbar - برندینگ و دکمه CTA
- **راهنمای پیاده‌سازی فنی:** در فایل `navbar.tsx`، متن تگ `<Link>` مربوط به لوگو را به «کدیار» تغییر دهید. سپس، متن و استایل دکمه CTA هدر را برای ایجاد شفافیت عمل‌گرایانه به «شروع کدنویسی (رایگان)» و لینک آن را به `#pricing` آپدیت کنید.
- **محدودیت‌های اختصاصی تسک:** هیچ کلاس Tailwind مربوط به ریسپانسیو بودن (مثل `md:flex`) را حذف نکنید. فقط محتوای متنی و تارگتِ لینک‌ها بروزرسانی شوند.
- **آرایه کانتکست ماشین‌خوان:**
```json
{
  "CONTEXT_FILES": ["components/ui/navbar.tsx"]
}
Task 2: بازنویسی ساختاری Hero Section
راهنمای پیاده‌سازی فنی: فایل hero-section.tsx را بازنویسی متنی کنید. متن Badge به «💡 خداحافظی با پرشِ کانتکست و فراموشیِ هوش مصنوعی»، تگ H1 به «از ایده تا خروجی نهایی. / بدون اینکه وسط راه گیر کنید.» (با حفظ استایل Gradient قبلی)، تگ P زیرتیتر به معرفی «حافظه‌ی یکپارچه پروژه» و بخش Social Proof به «همراه با جامعه‌ی پویای وایب‌کدرهای ایرانی» تغییر یابد.

محدودیت‌های اختصاصی تسک: ساختار تگ‌های Span داخل H1 برای حفظ تایپوگرافی و گرادیان رنگی (gradient-to-r) به شدت حفظ شود.

آرایه کانتکست ماشین‌خوان:

JSON
{
  "CONTEXT_FILES": ["components/sections/hero-section.tsx"]
}
Task 3: تبدیل منطق UI بخش Features به "آمار بهینه‌سازی پروژه"
راهنمای پیاده‌سازی فنی: تیترهای اصلی بخش features-section.tsx را به متون درخواستی (توسعه نرم‌افزار، بدون محدودیت‌های قبلی) تغییر دهید. دیتای کارت‌های چهارگانه را بازنویسی کنید:
۱. کارت Analytics را به «حافظه یکپارچه پروژه» و متریک‌ها را به (دقت تولید کد: ۹۸.۵٪، هزینه توسعه: ۱/۴، سرعت رسیدن به MVP: 3x) تغییر دهید.
۲. کارت Performance را به «بدون نیاز به تحریم‌شکن» با پایداری ۱۰۰٪ تبدیل کنید.
۳. کارت Shortcuts را به «تجربه‌ای آشنا و روان (شبیه Cursor)» تغییر دهید.
۴. کارت Integrations را به «درک کامل سرویس‌های بومی» تغییر دهید.

محدودیت‌های اختصاصی تسک: گرافیک‌ها و نمودارهای موجود در UI فعلی را حذف نکنید؛ صرفاً عناوین (Labels) و مقادیر (Values) آن‌ها را طوری بازنویسی کنید که با کانسپت "محیط توسعه و بهینه‌سازی" همخوانی داشته باشند، نه یک داشبورد فروش.

آرایه کانتکست ماشین‌خوان:

JSON
{
  "CONTEXT_FILES": ["components/sections/features-section.tsx"]
}
Task 4: استقرار استراتژی Anchoring در Pricing Section
راهنمای پیاده‌سازی فنی: تیتر بخش pricing-section.tsx را بروزرسانی کنید. آرایه plans موجود در کد را کاملاً با آرایه ۳ عضوی جدید (استارت‌آپ ۳۵۰هزار، پرو ۵۹۰هزار، BYOK ۲۵۰هزار) جایگزین کنید.

محدودیت‌های اختصاصی تسک: اطمینان حاصل کنید که پلن «پرو» با مقدار highlighted: true رندر شده و استایل متمایزی نسبت به دو پلن دیگر دریافت می‌کند (استراتژی لنگر انداختن). اگر کدی برای رندر پلن چهارم وجود دارد، منطق گرید (Grid) را از ۴ ستون به ۳ ستون (مثلاً lg:grid-cols-3) تعدیل کنید.

آرایه کانتکست ماشین‌خوان:

JSON
{
  "CONTEXT_FILES": ["components/sections/pricing-section.tsx"]
}
Task 5: بومی‌سازی Testimonials بر اساس پرسونای هدف
راهنمای پیاده‌سازی فنی: آرایه testimonials در فایل testimonials-section.tsx را با نظرات بومی‌سازی شده (امیررضا: وایب‌کدر، سارا: صاحب ایده، علی: توسعه‌دهنده فرانت، مریم: فول‌استک) که دقیقاً به دردهای فیلترینگ، توهم مدل و قطعی کانتکست اشاره دارند، جایگزین کنید.

محدودیت‌های اختصاصی تسک: آدرس تصاویر (Unsplash) حفظ شوند تا UI نشکند. فقط text، name و role در آرایه دیتای آبجکت‌ها ویرایش شود.

آرایه کانتکست ماشین‌خوان:

JSON
{
  "CONTEXT_FILES": ["components/sections/testimonials-section.tsx"]
}
Task 6: اصلاح فراخوان نهایی (CTA)
راهنمای پیاده‌سازی فنی: تیتر بخش cta-section.tsx را به «ایده‌ات را نصفه رها نکن.» تغییر دهید. پاراگراف توضیحات را با کپی‌رایتینگ مربوط به تمرکز بر خلق محصول جایگزین کنید.

محدودیت‌های اختصاصی تسک: دکمه ثانویه (درخواست دمو یا مشابه آن) باید کاملاً از DOM حذف شود. تنها دکمه اصلی <LiquidCtaButton> با لینک به #pricing و متن «شروع کدنویسی با کدیار» باقی بماند.

آرایه کانتکست ماشین‌خوان:

JSON
{
  "CONTEXT_FILES": ["components/sections/cta-section.tsx"]
}