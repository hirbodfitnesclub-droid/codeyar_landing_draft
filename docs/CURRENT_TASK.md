# CURRENT_TASK.md

## ۱. درخت تمرکز (Focus Tree)
- `components/sections/hero-section.tsx` (Scroll Logic)
- `app/page.tsx` (Layout consistency)

## ۲. جزئیات کامل تسک فعلی (Task 4)
**نام تسک:** مهندسی انیمیشن Zoom-through Scroll (میکرو-اینتراکشن)

**راهنمای پیاده‌سازی فنی:**
۱. در فایل `hero-section.tsx`:
   - یک `useTransform` بساز که `scrollY` را از بازه `[0, 500]` به مقیاس `scale` از `[1, 15]` تبدیل کند.
   - یک `useTransform` دیگر برای `opacity` بساز که از بازه `[200, 450]` اسکرول، شفافیت ترمینال را به `0` برساند.
   - گرید پس‌زمینه (`PerspectiveGrid`) نیز باید با اسکرول هم‌سوتر شود یا محو شود تا حس ورود به فضای جدید القا شود.
۲. کانتینر روتِ هیرو (`hero-root`) را به `sticky top-0` تبدیل کن و یک کادر نگهدارنده با ارتفاع مثلاً `h-[200vh]` دور آن قرار بده تا اسکرول مصنوعی ایجاد شود (Scroll triggering).

**محدودیت‌های اختصاصی تسک:**
- هماهنگی `scale` نهایی باید طوری باشد که وقتی کاربر به لبه‌های پیکسل می‌رسد، محو شدن کامل شده باشد.
- رعایت مطلق Anti-Patterns در PROJECT.md الزامی است.

## ۳. وضعیت تسک‌های بعدی (Future Pipeline)
- [x] تمام تسک‌های برنامه‌ریزی شده با موفقیت به اتمام رسیدند.

## ۴. رله کانتکست (Context Relay)
- **تسک ۳ انجام شد:** تیتر متحرک با افکت Decrypt و دکمه‌های متالیک در ترمینال جایگذاری شدند.
- **تسک ۲ انجام شد:** ترمینال شیشه‌ای با قابلیت Tilt 3D آماده است.
- **تسک ۱ انجام شد:** منطق رویدادهای ماوس و گرید پس‌زمینه نهایی شد.
- **نکته:** برای پیاده‌سازی Sticky Scroll، باید سکشن هیرو را در یک Div والد با ارتفاع زیاد قرار داد تا پنجره نمایش ثابت بماند اما مقدار `scrollY` افزایش یابد.
